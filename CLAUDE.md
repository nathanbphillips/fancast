# CLAUDE.md — FanCast

FanCast (working name, rename pending) is a live fan-commentary platform for football. A fan commentator broadcasts live audio for Arsenal matches; listeners watch their own TV/stream and get chat, live stats, a curated links feed, questions, call-ins, and a sync system that aligns commentary to their own feed. Full spec lives in `docs/`.

## Golden rules (never violate)

1. **Audio only, never the broadcast.** The platform must never transmit, embed, proxy, or link-unfurl-into-playback any match video or broadcast audio. This is the legal foundation of the entire product. No exceptions, including "just for testing."
2. **One phase at a time.** Work only on the current phase in `docs/PHASES.md`. Do not build ahead, do not refactor completed phases without being asked. Check boxes as acceptance criteria pass.
3. **iOS Safari is the certification gate** for all audio features. A feature is not done until it works there.
4. **Mobile-first.** Build the stacked mobile layout first, expand to the three-column desktop.
5. **DB is the source of truth, Ably is transport.** Every write goes through an API route that validates, persists to Supabase, then publishes to Ably. Clients must be able to reconstruct room state from the DB after reconnect.
6. **Never tick the clock over the wire.** Clock and room state are event-sourced (`clock_events`); clients render the running clock locally. See `docs/ARCHITECTURE.md`.
7. **Brand strings live in one config.** The product will be renamed. All user-facing brand strings, email sender names, and file-naming templates come from `lib/brand.ts`. Never hardcode "FanCast" in components.

## Stack (decided, do not re-litigate)

Next.js (App Router) + TypeScript strict + Tailwind on Vercel. Supabase (Postgres + Auth + Storage, RLS everywhere). LiveKit Cloud for audio (WebRTC + HLS egress + recording). Ably for chat/control channels. Sportmonks (v3 football) for match data. Stripe for tipping. Resend for email. Full rationale in `docs/ARCHITECTURE.md`.

## Conventions

- App Router with route groups: `(marketing)` for static pages, `(app)` for the product.
- Server components by default; client components only where interactivity demands.
- All Supabase access through `lib/db/` helpers; no inline queries in components.
- API routes in `app/api/`; every route validates input with zod before touching the DB.
- Design tokens are CSS variables defined once (`app/globals.css`) per `docs/DESIGN.md`; Tailwind references the variables. Both themes ship with every component.
- Tabular numerals (`font-variant-numeric: tabular-nums`) on anything that ticks: clock, score, stats, counts.
- Commit per working feature slice with a plain message. No giant commits at phase end.
- Tests: unit-test pure logic (clock derivation, sync offset math, flag weighting, marker merging, segment boundary computation). Don't chase UI coverage in MVP.

## Decision log (defaults the founder can override)

| Decision | Value | Status |
|---|---|---|
| Audio platform | LiveKit (Agora documented fallback) | Final |
| Email provider | Resend | Default |
| One-time tip presets | $3 / $5 / $10 / custom | Default |
| Monthly support tiers | $3 / $5 / $10 | Default |
| Platform fee on tips | 10%, shown transparently in tip sheet | Placeholder, founder to confirm before Phase 9 |
| Segment cutting | Vercel function + ffmpeg-static, MP3 stream-copy; move to small worker only if limits hit | Default |
| Waiting-room audio | Not recorded; recording starts at Start Broadcast | Final |
| Recording rights | Commentator owns 100%, no platform license, no exclusivity | Final (policy) |
| Dump button / broadcast delay for callers | Not built | Final |
| Chat vote arrows | Visible on every message, always | Final |
| `fixtures` table (Sportmonks cache; absent from ARCHITECTURE data model); dev seeds use negative ids, purged on first real sync | Added Phase 2 | Assumed |
| Theme conflict: explicit device choice (localStorage) beats account `theme_pref`; account pref fills in on devices with no choice | Phase 2 | Assumed |
| Fixtures sync trigger: admin-only POST `/api/fixtures/sync`; scheduled trigger deferred to Phase 7 | Phase 2 | Assumed |
| Vote/flag aggregates denormalized onto `chat_messages`/`links` rows, recomputed from vote rows on every write | Phase 3 | Assumed |
| Flag weight "established" = account ≥48h AND standing good; the "no session history" criterion waits for `listener_segments` (Phase 9) | Phase 3 | Assumed |
| Blocklist matches the exact domain and all subdomains | Phase 3 | Assumed |
| Hide log = `hidden_by`/`hidden_at` columns + flag rows; no separate log table | Phase 3 | Assumed |
| PostgREST embeds need explicit FK hints (`profiles!<fk_name>`) since junction tables (votes/flags) make joins ambiguous | Phase 3 | Technical note |
| Link cards are **compact**: title + domain + votes on the left, a small (80px) square thumbnail on the right (no big hero image / description). Reverses the 2026-06-11 "rich preview, wide image" decision — those dwarfed the chat. | Founder decision 2026-06-29 | Final |
| Waiting-room countdown targets a commentator-set broadcast start time (`rooms.broadcast_start`), not kickoff; no time set = calm "show starts soon" card | Founder decision 2026-06-11 | Final |
| Commentator can open chat and/or links to listeners during waiting (`rooms.chat_open`/`links_open` toggles), relaxing FR-3.2's commentator-only waiting chat | Founder decision 2026-06-11 | Final |
| Stats panel shows zeros (possession 50/50) until live Sportmonks data arrives (Phase 7) | Founder decision 2026-06-11 | Final |
| Ending a call is neutral (no profile/eligibility effect), replacing FR-4.4's "never removed from air" gate; problem callers handled by commentator-only `caller_flags` (informational, shown on request cards) + reversible `call_in_blocks` (bars call-ins only) | Founder decision 2026-06-11 | Final |
| Match-data provider: **Sportmonks** (v3 football) — replaces API-Football after account issues. Env `SPORTMONKS_API_TOKEN`/`SPORTMONKS_BASE`; Arsenal team id 19, EPL league id 8 (in `lib/config.ts`); sync fetches `/fixtures/between/{start}/{end}/{teamId}`. Plan must cover the English Premier League. | Founder decision 2026-06-16 | Final |
| Match-stats proxy: public `GET /api/stats/[fixtureId]` (reading is open) with a hardcoded Sportmonks include and a `globalThis` TTL(10s) cache + in-flight coalescing + last-good fallback; seed/invalid id (≤0) returns the zeros contract with no upstream call | Phase 7 | Assumed |
| `stats_tab` is a transient control-channel hint (commentator→listeners via `POST /api/stats-tab`), not persisted (no DB row); recovered on reconnect via the control channel's rewind window; a listener's local tab tap overrides until the next push | Phase 7 | Assumed |
| Desktop room layout is a personal, local choice (not commentator-pushed, not yet saved): default = stats 25 / chat 50 / links 25; "Expand stats" → stats 50 / chat 50 (chat is priority, stays); "Expand links" → stats 50 / links 50 with **chat behind links**; a `[Chat \| Links]` companion toggle swaps the right half; Minimize returns to default. Stats is the anchor when expanded; chat is the middle panel squeezed out. Replaces the old commentator-only collapse rails. | Founder decision 2026-06-22 | Final |
| Deeper stats (xG, momentum, player ratings open by default; goalkeepers, per-half, game state, extended-24 collapsed) show inline on desktop **only when expanded to 50%**, but **always** stack below the 13 on mobile (collapsible "Match stats" + "Advanced" groups; no 50% expand on mobile) | Founder decision 2026-06-22 | Final |
| Listener volume runs through the audio engine: a Web Audio **gain node** on the live sync path (the only volume iOS Safari honours — element `.volume` is a no-op there); radio + no-worklet fallback set element `.volume` (effective on desktop, hardware on iOS). Mobile exposes it in a collapsible audio drawer (play/pause · score+clock · caret → sync · radio · volume), closed by default | Founder decision 2026-06-22 | Final |
| **Room overhaul (Phase 11).** Desktop = stats LEFT **1/3** / merged chat+links stream RIGHT **2/3** (`lg:grid-cols-[1fr_2fr]`; was 50/50, widened to 2/3 chat per founder 2026-06-28). **Supersedes** the 2026-06-22 25/50/25 + expand/swap/companion layout and the "deeper stats inline only when expanded to 50%" entry (deeper stats are always inline on desktop; mobile unchanged). | Founder decision 2026-06-24 | Final |
| Chat + links **merge into one stream**; a per-device (localStorage) filter switches chat-only / links-only / **blended (default)**. Blended = chronological interleave by `created_at`. Links keep their own table + Ably channel; the client merges the two channels (no unified `stream_items` table). Relaxes FR-9.4 "identical feed" (filter is local, not role-based). Mobile loses the Links tab; the filter becomes in-stream chips. | Founder decision 2026-06-24 | Final |
| **Threaded replies, deep Reddit-style.** `parent_id`/`root_id`/`depth` added to `chat_messages`; replies are `chat_messages` rows on the existing `:chat` channel (inherit the 3-per-6s send throttle). Beyond ~depth 3-4 the inline tree collapses to "View N more replies" → a **focused single-thread view** (permalink pattern). Replies are votable + flaggable; flags share the 10/match budget. **Supersedes** FR-8.1 "flat chronological" (now: chronological top-level items with nested reply subtrees). | Founder decision 2026-06-24 | Final |
| **Voting becomes a ranking lever** with sort modes: **chronological (default) / top (net upvotes) / controversial (up:down balance)**, mirroring Reddit. Live re-sort only in chronological (keeps pin-to-bottom + "N new" pill); top/controversial **freeze-and-batch** — counts update live but the order re-ranks only on an explicit "N new — refresh". **Supersedes** FR-8.2 "social signal only; never affects ordering/visibility." **Therefore mandatory:** per-user vote rate-limiting + established-account weighting (reuse the `[[flag-weight]]` ≥48h/good-standing notion) on `/api/chat/vote` + `/api/links/vote` before sort ships. | Founder decision 2026-06-24 | Final |
| Inline link-card votes are **social-signal only** (drop FR-9.2's 2:1+5 downvote auto-hide for inline links); moderation hides links via flags/commentator only. **Pre-req:** bring `links` to RLS redaction parity with chat's migration 0011 (hidden url/og not readable via anon) + a real link-hide realtime event, **before** the merge ships. | Founder decision 2026-06-24 | Final |
| In-chat interactive widgets (Poll, Score Predictor, Player Ratings, Preference Slider, InteractionButtons) currently render inside `LiveChat`; the overhaul must give them an explicit home in the new stream column (not orphan them). | Phase 11 | Assumed |
| **Landing + room visual redesign** (founder mockups): front-end ONLY, all data/behaviour kept in place. New **Space Grotesk** display font (`--font-display`, headings/labels/wordmark only — never on tabular-nums to avoid jitter); `--shadow-card`/`--shadow-raised` tokens both themes. Landing restyled (display hero, gold-bordered featured card with red Join, multi-column footer); **functional-only** — omit mockup chrome with no built feature (notification bell, Become-a-Commentator CTA, View-full-schedule link, home per-card listener counts, Team:Arsenal pills). **Room top bar merged into ONE**: global header extracted to client `AppHeader` (self-hides on `/room/` like `SiteFooter`), `MatchHeader` carries wordmark · score pill · clock · LIVE · listeners · theme · user menu (chrome passed as slots from `RealtimeRoom`, score stays client-side); room root → `h-dvh`; scheduled-room card gets a minimal wordmark header. Stats panel: shadow cards, uppercase display TABLE/TEAM-NEWS labels, red(home)/navy(away) team-name underlines. **Avatars** on chat messages + nav (`profiles.avatar_url` added to every `author:profiles(...)` embed; `components/Avatar.tsx`, hashed initial-circle fallback). Chat structure (combined Refresh+sort header, no Chat/Links filter, inline link cards, reply-on-the-right, threading) **preserved, not reverted**. | Founder decision 2026-06-29 | Final |
| **Marketing landing + About page.** `/` becomes a **hybrid landing** (hero + `HeroArt` SVG → how-it-works → live + **next-4** fixtures teaser w/ "View full schedule" → features → CTA band); the FULL schedule moves to new **`/matches`**; new **`/about`** deep-dive + FAQ. Shared `lib/db/fixtures.ts` `loadFixtures()` feeds both `/` and `/matches`. New presentational primitives in `components/marketing/` (`Section`/`StepCard`/`FeatureCard`/`CtaBand`/`Faq`/`CtaButtons`/`HeroArt`) reuse tokens — no new CSS. `AppHeader` gains a `hidden sm:flex` Matches/About nav; `SiteFooter` "Matches"→`/matches` + About. Default OG card via `app/opengraph-image.tsx` (`next/og`, vector only). **Positioning: Arsenal-first**, light "more clubs coming" + small "host a room" mention. **Copy compliance (load-bearing):** never imply we stream/show the match or broadcast audio ("watch" only ever = the user's own stream; we "listen alongside"); never imply official club/league/broadcaster affiliation; explicit "we don't show the match" + FAQ kept prominent; unaffiliated disclaimer rides the shared footer. NO club crests/kits/player/broadcast imagery — illustrations are abstract SVG. | Founder decision 2026-06-30 | Final |
| In-room Q&A surfacing: NONE — the commentator answers questions on air or in chat; no pinned answered-question card or answer field (drops the Cloud Design "Asked the commentator" card) | Founder decision 2026-07-02 | Final |
| Desktop chat column gains a **Polls tab** (Room chat \| Polls \| commentator-only Questions): poll + score predictor + player ratings live there, freeing chat height. Mobile: predictor + poll stay in chat; **ratings render under STATS**. **Player ratings open at HALF-TIME (rate the first half) and again POSTGAME (rate the whole game; scores revisable)** — replaces postgame-only FR-12.3 | Founder decision 2026-07-02 | Final |
| Avatars render via next/image (`images.remotePatterns` https any) so a hostile avatar host can't harvest room participants' IPs (tracking pixel); admins clear any avatar via `POST /api/admin/clear-avatar` (button on `/u/[username]`). Proper fix (upload bucket) still planned | Audit fix 2026-07-02 | Assumed |
| **Commentator Platform Epic** (spec `..\Commentator Platform Epic\`, FR-18 to FR-26, Phases 12-15, migrations 0026-0032): commentator accounts become **self-serve** with terms acceptance + version recorded (supersedes FR-2.2 manual grant; admin suspend as backstop, never announced) | Founder decision 2026-07-03 | Final |
| Profile URLs move to **root `/{username}`** (reserved-username list; `/u/[username]` 301s); one profile page per person, commentator sections (about 280ch, fixed-platform social links, upcoming rooms, reserved stats strip) layer onto it | Founder decision 2026-07-03 | Final |
| Room URLs: **immutable slug** `{home}-vs-{away}-{dd-mmm-yyyy}-{host}` (Europe/London date), `/room/{id}` 301s; creation inputs are exactly broadcast start (default kickoff minus 15 min) + optional 140ch blurb; v1 rooms only for API-listed fixtures; **sync expands to all EPL fixtures** (positioning stays Arsenal-first) | Founder decision 2026-07-03 | Final |
| **Attendance**: explicit RSVP ("Count me in"), count string "XX people planning to attend", live rooms "XX listening now"; **copy rule is load-bearing: "attend" only ever attaches to the room, never the match** (all strings from `lib/strings/attendance.ts`); no public attendee list (friend chips only to accepted friends) | Founder decision 2026-07-03 | Final |
| **Co-hosts are equals** (no primary; cap 2 in v1, `room_hosts` schema supports N and lands in PRD-02); every host check routes through one `isRoomHost(userId, roomId)` helper; tips: listener picks the recipient host, no payout splitting; both hosts get full recording rights | Founder decision 2026-07-03 | Final |
| **Fan score** = max(0, comments + net weighted votes), floored at zero, components stored for future badges; matches attended at 15+ min in listener_segments | Founder decision 2026-07-03 | Final |
| **Friends**: double opt-in, silent decline (no re-request after decline), blocking ships in the same PRD (severs friendship, invisible to the blocked); no DMs | Founder decision 2026-07-03 | Final |
| **Notifications v1**: email (Resend) + web push (PWA), no in-app inbox; batching is core design, dedupe key **(recipient, room, type)**; bulk actions collapse to one summary per recipient; every email has signed one-click unsubscribe | Founder decision 2026-07-03 | Final |
| Matches page goes date-grouped with multi-room fixture rows now; filters/search/team pills deferred until room volume demands them. Never use an emdash in any document or user-facing string | Founder decision 2026-07-03 | Final |
| Epic infra rulings: **build around Resend + Stripe** (domain not picked yet; both prepped for drop-in: `lib/notify/email.ts` no-ops without RESEND_API_KEY, tip rows carry recipient_user_id from day one); **all scheduling fits Vercel Hobby** (once-daily crons only): immediate notifications send inline after the response, reminders drain opportunistically from high-traffic routes + daily sweep, fixture sync = daily cron + stale-picker refresh + manual trigger | Founder decision 2026-07-03 | Final |
| **Avatar uploads** (self-serve): `POST/DELETE /api/profile/avatar` is the ONLY way to set an avatar (free-text `avatar_url` dropped from the profile PATCH). Owner-only, 12/hr, magic-byte sniff (PNG/JPEG/WebP; SVG rejected), 30M-px decompression cap, `sharp` re-encode to a 256px WebP in the public `avatars` bucket (strips metadata). `MAX_UPLOAD_BYTES` = 4 MB (under Vercel's 4.5 MB body limit). `next/image` `remotePatterns` pinned to the Supabase Storage host (was any-https) | 2026-07-05 | Assumed |
| **Front-end review pass** (founder-ranked, 2026-07-05): pre-launch honesty over faked momentum. Deleted fabricated signals (hero "1,204", named fake hosts/@handles -> archetypes, aspirational stat); OnAirCard relabelled "Preview" + generic host; real founder section (Nathan Phillips, from bsky) + footer/bsky contact + "Early access" chip. Category stance in `brand.tagline` ("the matchday room", "turn the pundits off"). Funnel: state-aware hero CTA, "free to listen" promoted, "One second screen" differentiation (audio+chat+stats in one) vs Spaces/YouTube/group-chat, varied CTAs. **`/host` is now role-aware** (marketing `HostLanding` for signed-out/listeners, dashboard for commentators); host content moved OUT of `/how-it-works` (now listener-only). Mobile hamburger nav in `AppHeader`. Home FAQ strip. **Email capture**: migration 0036 `waitlist` (RLS-on, no anon policy, service-role writes) + public `POST /api/waitlist` (IP-limited) + `NotifyForm` in empty states = launch list. SEO: `app/robots.ts` + `app/sitemap.ts` + Organization JSON-LD + home metadata; single `themeColor` #0f0f11. RSVP intent carried through signin via sanitized `next`. **Room sharing** (founder: "easy sharing of matchday rooms!"): rich per-room OG metadata + dynamic `room/[id]/opengraph-image.tsx` card + `ShareButton` on match rows. **Type system (item 18)**: a locked modular scale (`.t-hero/.t-h2/.t-h3/.t-title/.t-lead`, fluid clamp) replaces per-instance `text-[NNpx]`; `--gold-bright` (vivid decorative gold) split from text `--gold` (AA `#9a7714` in light); `--section-y` rhythm. Deferred: favicon (awaiting final logo), rename (keeping FanCast), and big-swing hero-audio/sync-demo/demo-room | Founder decision 2026-07-05 | Final |
| **Custom rooms** (rooms agnostic of listed fixtures): `/host/new` gains "Create your own room" with host-set **title** + **start time** (immediate = waiting room opens directly; future = scheduled). Typing the title hits `GET /api/fixtures/search` (Sportmonks suggest via team-search + fixtures/between, commentator-gated, 60s cache in `lib/fixtureSearch.ts`); picking a suggestion links the real fixture (server re-fetches by id, insert-if-absent so a shared fixture is never overwritten) so stats flow. Unlinked rooms: title MUST parse as "Home vs Away" (keeps slug/scoreboard/auto-matcher working; noted as Assumed), deduped onto an existing fixture within 24h before minting a collision-retried synthetic admin fixture, friendly "stats may not be available + more leagues soon" note, and a free-text **league request** box (`league_requests`, migration 0037; free text on purpose, a dropdown across sports would overwhelm). Flood control: 10/hr rate limit + 100 active-room backstop. **The redesign had orphaned `OpenWaitingButton`, stranding every scheduled room (picker included); restored via a host "Open waiting room" control on `/room/{slug}` and dashboard row links.** No-show sweep now waits for BOTH kickoff+2h and broadcast_start+2h; cancels purge unsent reminders | Founder decision 2026-07-06 | Final |
| **Room-cancel notifications wired** (single/bulk/unsubscribe → `notifyRoomsCanceled`: room_change("canceled") to RSVP holders + co-hosts, actor excluded, per-room; one-summary-per-recipient collapse for bulk is a follow-up). Surfaced by the /host/guide handbook fact-check: the claim existed before the wiring did | 2026-07-06 | Assumed |

## Where things are specified

- Requirements and acceptance criteria: `docs/PRD.md`
- Data model, channels, routes, audio pipeline: `docs/ARCHITECTURE.md`
- Tokens, components, clock/state rules: `docs/DESIGN.md`
- Build order and current status: `docs/PHASES.md`
- Content for /guidelines, /terms, /privacy, consent copy: `docs/LEGAL_PAGES.md`

## When the spec is silent

Make the smallest reasonable choice, note it in the Decision log above with status "Assumed", and continue. Ask the founder only when a choice is user-visible and hard to reverse (payments, data retention, anything touching the golden rules).
